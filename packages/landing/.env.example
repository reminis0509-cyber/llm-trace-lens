# LINE LIFF ID — obtained from the LINE Developers console. Optional:
# when unset, LIFF SDK init is skipped and the LP works normally in a
# regular browser. Set for LINE in-app browser demo flows (/liff/tutorial).
VITE_LINE_LIFF_ID=
