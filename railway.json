{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Build timestamp:' $(date) && npm run build && cd packages/dashboard && rm -rf dist node_modules node_modules/.vite && npm install && npm run build && echo 'Dashboard assets:' && ls -la dist/assets/"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
